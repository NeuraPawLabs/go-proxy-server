# 安全策略

[English](SECURITY.md)

## 重点关注范围

以下模块的安全问题尤其值得优先报告：

- 认证与凭证处理
- IP 白名单逻辑
- 代理请求校验与 SSRF 防护
- 内网穿透鉴权、TLS 与路由隔离
- 仅监听本地的 Web 管理接口

## 密码哈希设计决策

代理用户凭证采用带版本标识的 `$sha256$<salt>$<digest>` 格式，其中盐值由密码学安全随机源生成，长度为 32 字节，哈希执行单轮 SHA-256。这是有意的性能取舍：代理认证可能发生在每次新建连接时，自适应密码 KDF 会把额外 CPU 开销与延迟直接引入连接建立过程。每个凭证使用独立随机盐，可避免跨凭证复用预计算结果；摘要校验使用常量时间比较。

仅监听 localhost 的 Web 管理员账号使用独立的 Argon2id 格式（`m=65536`、`t=3`、`p=2`）。管理员登录频率较低，可以承担额外计算开销。已有的管理员 SHA-256 哈希会在下一次成功登录后自动迁移为 Argon2id。

因此，本项目接受该取舍所依赖的运行模型包括：

- 代理用户使用足够长、唯一且高熵的密码；管理员密码即使由 Argon2id 保护，也应保持足够强度
- 应用数据目录、数据库及其备份仅允许可信运维人员访问
- Web 管理服务保持仅监听 localhost；如需远程访问，应由独立的安全访问层提供保护

安全 review 不应仅因代理用户使用单轮随机盐 SHA-256 就将其视为意外遗漏，而应把它作为已有文档记录的设计决策。若发现认证绕过、盐值可预测或复用、凭证或哈希泄露、未使用常量时间校验，或能证明超出上述运行模型的实际影响，仍属于有效安全问题。

## 如何报告漏洞

如果你认为发现了安全漏洞，请先私下联系维护者，不要第一时间公开提 issue。

建议提供：

- 受影响的版本或提交
- 风险影响说明
- 复现步骤或 PoC
- 可行的缓解建议（如果有）

## 披露建议

- 请给维护者留出复现、修复和验证的时间。
- 在修复或缓解方案可用前，避免公开披露细节。
- 涉及安全行为变化时，应同步更新相关文档。
